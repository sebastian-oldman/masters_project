\appendix
\chapter{Provenance of Figures and Quoted Numbers}
\label{app:provenance}

Every figure in this report is drawn by a script from files in \texttt{data/processed/}, which are themselves produced from raw downloads recorded in \texttt{data/raw/manifest.csv} with their URL, access date and SHA-256. The tables below list, for each figure, the processed files it is drawn from, the raw sources behind them and the code that produces them. Headline values quoted in the text are printed on the figures or appear in the chapter tables; the few numbers that appear only in prose (source-document figures such as EPRI's state total, and ratios derived from tabulated values) are listed with their sources in \texttt{docs/DATA\_NOTES.md}, which also records every data limit found during the audits. Rebuilding is one command per chapter (\texttt{make ch1}, \texttt{make ch2}, \texttt{make ch3}, \texttt{make ch4}); the standalone bundle reproduces every output byte for byte.

\begin{table}[H]
\centering\scriptsize
\caption{Chapter~\ref{ch:current} figures: processed files, raw sources and code.}
\resizebox{\textwidth}{!}{\input{tables/provenance_ch1}}
\end{table}

\begin{table}[H]
\centering\scriptsize
\caption{Chapter~\ref{ch:growth} figures: processed files, raw sources and code.}
\resizebox{\textwidth}{!}{\input{tables/provenance_ch2}}
\end{table}

\begin{table}[H]
\centering\scriptsize
\caption{Chapter~\ref{ch:supply} figures: processed files, raw sources and code.}
\resizebox{\textwidth}{!}{\input{tables/provenance_ch3}}
\end{table}

\begin{table}[H]
\centering\scriptsize
\caption{Chapter~\ref{ch:together} figures: processed files, raw sources and code.}
\resizebox{\textwidth}{!}{\input{tables/provenance_ch4}}
\end{table}

\chapter{Manifest Rows Behind the Figures}
\label{app:manifest}

Each figure's source line names manifest ids. This appendix prints the manifest row behind every id that any figure names: owner, access date, SHA-256 and URL, as recorded in \texttt{data/raw/manifest\_frozen\_2026-09-22.csv} at the data freeze. Files that come in dated series (the EIA-930 half-year files, the EIA-923, EIA-860 and EIA-860M vintages, the QCEW quarters, the ERCOT newsletters, the CBRE Infogram tables) are listed on one line each with their count and the URL of the first file; the CAISO Today's Outlook ids stand for directories of daily files whose own manifest sits beside them. The machine-readable list is \texttt{data/processed/ch5\_manifest\_rows\_used.csv}, and the appendix build fails if any source label in a figure does not resolve to a manifest row.

{\scriptsize\input{tables/manifest_rows_used}}
